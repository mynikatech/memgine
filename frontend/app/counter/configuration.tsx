import Placeholder from "@/src/components/Placeholder";

export default function StaffConfiguration() {
  return (
    <Placeholder
      testID="staff-configuration-screen"
      icon="settings-outline"
      title="Configuration"
      description="Staff configuration shell. Business setup and settings arrive in a later stage."
    />
  );
}
