# Copy to backend.hcl after the production Terraform state bucket is approved.
bucket       = "memgine-prod-terraform-state-REPLACE_ACCOUNT_ID"
key          = "prod/terraform.tfstate"
region       = "ca-central-1"
encrypt      = true
use_lockfile = true
profile      = "REPLACE_WITH_PRODUCTION_AWS_PROFILE"
